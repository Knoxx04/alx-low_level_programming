#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: Print single digit base 10 numbers starting from 0
 *
 * Return: Always 0 (succes)
 */
int main(void)
{
	int num;

	for(num = 0; num < 10; num++)
	{
		printf(num);
	}
	return (0);
}
