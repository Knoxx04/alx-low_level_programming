#include "main.h"
/**
 * main - Entry point
 *
 * Description: Print the alphabet in lowercase followed by a newline
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char min = 'a';

		while (min <= 'z')
		{
			min++;
		}
	_putchar('\n');

		return (0);
}
